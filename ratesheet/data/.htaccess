# Block all direct HTTP access to this folder — it holds the Customer
# Rate Sheet Sign Up app's SQLite database (rep accounts, sent rate
# sheets, and the customer data those signups collect). Nothing here
# should ever be served to a browser; only ratesheet/api/*.php reads/
# writes it via PDO on the server side.
Order allow,deny
Deny from all
